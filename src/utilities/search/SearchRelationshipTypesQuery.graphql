query SearchRelationshipTypesQuery($search: String) {
  relationshipTypes(search: $search) {
    edges {
      node {
        id
        toPersonMaleDisplayName
        toPersonFemaleDisplayName
      }
    }
  }
}
