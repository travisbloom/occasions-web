query AssociatedEventDetailsQuery($associatedEventId: ID!) {
  associatedEvent(id: $associatedEventId) {
      id
      receivingPerson {
        id
        fullName
      }
      transactions {
        edges {
          node {
            id
            costUsd
            product {
              id
              name
              mainImageUrl
              description
            }
          }
        }
      }
      event {
        id
        name
        dateStart
        timeStart
        isReoccuringYearly
        relatedProducts {
            edges {
              node {
                name
                description
                id
                slug
              }
            }
          }
      }
  }
}
