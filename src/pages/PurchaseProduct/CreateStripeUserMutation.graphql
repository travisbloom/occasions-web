mutation CreateStripeUserMutation($input: CreateStripeUserInput!) {
  createStripeUser(input: $input) {
      user {
          username,
          accessTokens {
            edges {
              node {
                id
              }
            }
        }
      }
  }
}
