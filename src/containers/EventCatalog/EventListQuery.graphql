query EventListQuery($eventSearchValue: String, $eventTypes: [ID]) {
  events(eventTypes: $eventTypes, search: $eventSearchValue) {
    edges {
      node {
        id
        name
        slug
        dateStart
        timeStart
        isReoccuringYearly
      }
    }
  }
}
